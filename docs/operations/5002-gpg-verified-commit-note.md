# GPG verified commit note

Before pushing maintenance batches, confirm the local author email matches a verified UID on the registered GitHub GPG key.

Verification: run `git log -1 --show-signature` and check GitHub shows the commit as verified after push.
