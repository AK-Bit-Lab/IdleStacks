# README owner reference note

Clone URLs and issue links should point to `AK-Bit-Lab/IdleStacks` so setup instructions match the active repository owner.

Verification: search docs for stale owner strings before release notes are published.
