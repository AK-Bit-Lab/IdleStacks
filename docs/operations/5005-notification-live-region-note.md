# Notification live region note

Toast containers should use a polite live region by default, reserving assertive alerts for error states only.

Verification: submit one success and one error notification and confirm announcement order is not noisy.
