# Theme storage fallback note

Theme initialization should tolerate unavailable storage and fall back to system preference before applying the default theme.

Verification: test in private browsing and with localStorage disabled where possible.
