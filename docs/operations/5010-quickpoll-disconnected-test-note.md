# QuickPoll disconnected test note

QuickPoll tests should cover the disconnected wallet path so contract calls are not attempted without a wallet session.

Verification: run the QuickPoll hook test file after changing wallet guards or action labels.
